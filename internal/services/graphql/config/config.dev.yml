gin:
  port: ":4000"
  host: ""

grpc-client:
  services:
    - name: "auth_service"
      port: ":5101"
      host: "auth_service"
    - name: "health_service"
      port: ":5102"
      host: "health_service"